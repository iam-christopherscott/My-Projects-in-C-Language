{
    // Calculate celcius to farenheit with user input (4 = 39.20, 5 = 41)
    //(0°C × 9/5) + 32 = 32°F
    int c;
    printf("Enter the value in Celcius\n");
    scanf("%d", &c);
    float f = (c * (9.0/5.0)) + 32;
    printf("The value of temperature in farenheit is %f\n", f);
    return(0); //returning value to int main()
}
