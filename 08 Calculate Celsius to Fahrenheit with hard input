//Formula for computing celcius to farenheit
//(0°C × 9/5) + 32 = 32°F

{
    int c = 4;
    float f = (c * (9.0/5.0)) + 32; // We need to ensure that the values inside the bracket (9/5) is a float by making it (9.0/5.0) to avoid errors.
    printf("The value of temperature in farenheit is %f", f);
    return(0); //returning value to int main()
}
